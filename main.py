#!pcsx2py

# This non-volatile module `monitor.main` is loaded on:
# - pcsx2 startup after logging `Python initialized.` message.

import sys
import os

sys.path.append(os.path.dirname(__file__))
